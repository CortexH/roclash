-- Assinaturas de contexto para os modos de seleção.
-- Usadas tanto nos inputs (PointingService) quanto no evento de hover
-- (UnitHoverClient), para identificar quem originou a interação/hover.
return {
	UNIT_SELECT = "UNIT_SELECT",
	UNIT_MOVE_SELECT = "UNIT_MOVE_SELECT",
	UNIT_ATTACK_SELECT = "UNIT_ATTACK_SELECT",
}
